<UserControl x:Class="SpotifyWPF.View.Component.CollapsibleActivityLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SpotifyWPF.View.Component"
             x:Name="Root"
             MinHeight="28">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style x:Key="ActivityHeaderToggleStyle" TargetType="{x:Type ToggleButton}">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Background" Value="{StaticResource AppSurfaceAltBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AppBorderBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource AppForegroundBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border x:Name="HeaderBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="0,0,0,1"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="HeaderBorder" Property="Background" Value="{StaticResource AppHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="HeaderBorder" Property="Background" Value="{StaticResource AppSurfaceBrush}"/>
                                <Setter TargetName="HeaderBorder" Property="BorderBrush" Value="{StaticResource SpotifyGreenBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{StaticResource AppSurfaceBrush}"
            BorderBrush="{StaticResource AppBorderBrush}"
            BorderThickness="1">
        <DockPanel>
            <ToggleButton DockPanel.Dock="Top"
                          Style="{StaticResource ActivityHeaderToggleStyle}"
                          IsChecked="{Binding IsExpanded, ElementName=Root, Mode=TwoWay}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Activity"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"
                               Foreground="{StaticResource AppForegroundBrush}"/>
                    <TextBlock Margin="8,0,0,0"
                               FontFamily="Segoe UI Symbol"
                               VerticalAlignment="Center"
                               Foreground="{StaticResource AppMutedForegroundBrush}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="▶"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsExpanded, ElementName=Root}" Value="True">
                                        <Setter Property="Text" Value="▼"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </ToggleButton>

            <local:ActivityLogView MinHeight="100"
                                   Margin="8"
                                   DataContext="{Binding ActivityLog, ElementName=Root}"
                                   Visibility="{Binding IsExpanded, ElementName=Root, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"/>
        </DockPanel>
    </Border>
</UserControl>
