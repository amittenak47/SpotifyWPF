<UserControl x:Class="SpotifyWPF.View.Component.ActivityLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="120" d:DesignWidth="800"
             VerticalAlignment="Stretch"
             VerticalContentAlignment="Stretch">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="ActivityLogBooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    Style="{StaticResource ControlPanelToolbarStyle}"
                    HorizontalAlignment="Right"
                    Visibility="{Binding ShowFilter, Converter={StaticResource ActivityLogBooleanToVisibilityConverter}}">
            <TextBlock Text="Filter"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0"
                       Foreground="{StaticResource AppMutedForegroundBrush}"/>
            <ComboBox ItemsSource="{Binding LogFilterOptions}"
                      SelectedItem="{Binding SelectedLogFilter}"
                      Style="{StaticResource DarkComboBoxStyle}"
                      Width="120"
                      VerticalAlignment="Center"
                      Padding="6,2"/>
        </StackPanel>

        <ListBox Grid.Row="1"
                 x:Name="LogMessagesListBox"
                 ItemsSource="{Binding LogMessages}"
                 Background="Transparent"
                 BorderThickness="0"
                 FontFamily="{Binding FontFamily}"
                 FontSize="{Binding FontSize}"
                 SelectionMode="Extended"
                 VerticalAlignment="Stretch"
                 MinHeight="72"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ContextMenu>
                <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                    <MenuItem Header="Copy Selected"
                              Command="{Binding CopySelectedLogMessagesCommand}"
                              CommandParameter="{Binding PlacementTarget.SelectedItems, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                    <MenuItem Header="Copy All"
                              Command="{Binding CopyAllLogMessagesCommand}"/>
                </ContextMenu>
            </ListBox.ContextMenu>
        </ListBox>
    </Grid>
</UserControl>
