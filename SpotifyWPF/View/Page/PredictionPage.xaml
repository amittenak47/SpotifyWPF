<UserControl x:Class="SpotifyWPF.View.Page.PredictionPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             DataContext="{Binding PredictionPage, Source={StaticResource Locator}}"
             d:DesignHeight="450" d:DesignWidth="800">

    <DockPanel Margin="8" Background="{StaticResource AppBackgroundBrush}">
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <Label Content="{Binding Status}"/>
            </StatusBarItem>
        </StatusBar>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="520">
            <TextBlock Text="Prediction"
                       FontSize="22"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,12"/>
            <TextBlock TextWrapping="Wrap"
                       TextAlignment="Center"
                       Foreground="{StaticResource AppMutedForegroundBrush}"
                       Text="Planned: infinite looping (Infinite Jukebox-style beat graph) and personalized next-track prediction. This page is a scaffold for upcoming experimental playback features."/>
        </StackPanel>
    </DockPanel>
</UserControl>
